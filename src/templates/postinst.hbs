#!/bin/sh

error_exit()
{
  echo "${1:-"Unknown Error"}" 1>&2
  exit 1
}

create_symlink()
{
  if [ -f {{ install_dir }}/bin/crystal ]; then
    ln -sf {{ install_dir }}/bin/crystal /usr/local/bin || error_exit "Failed to create symlink in /usr/local/bin"
  fi
}

create_symlink